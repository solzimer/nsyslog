## Timestamp Processor

Places a timestamp to the input object

## Examples

```json
"processors" : {
	"timestamp" : {
		"type" : "timestamp",
		"config" : {
			"field" : "timestamp"
		}
	}
}
```

## Configuration parameters
* **field** : Field where timestamp is stored
